# 定时更新 clash-aio 订阅：每天 4 点执行 clash-subscription-hot-reload.sh
# 将下面一行加入 crontab -e，并把 /path/to/clash-aio 改为实际项目目录
# 0 4 * * * cd /path/to/clash-aio && ./clash-subscription-hot-reload.sh >> /tmp/refresh-clash.log 2>&1
